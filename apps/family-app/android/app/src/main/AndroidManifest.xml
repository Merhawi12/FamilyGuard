<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Backup is off, and both switches are set because they cover different
      Android versions: `allowBackup` governs cloud backup everywhere, and
      `dataExtractionRules` governs it *and* device-to-device transfer from
      Android 12 on. See res/xml/data_extraction_rules.xml for why it matters
      here specifically — this app is a WebView, and the parent's session token
      lives in its localStorage inside the backed-up data directory.
    -->
    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Android 13 and later will not show a notification until the app has asked
      for this and been granted it. @capacitor/push-notifications declares it in
      its own manifest and the merger would pull it in regardless, but it is
      stated here too: this file is what ships, and a permission that only exists
      inside a dependency is one nobody reviewing the app can see.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
