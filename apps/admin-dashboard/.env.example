# Origin of the Parentix API. Leave blank locally so the Vite dev proxy
# (see vite.config.js) forwards /api and /socket.io to http://localhost:5000.
VITE_API_URL=

# Production example:
# VITE_API_URL=https://api.parentix.ca
